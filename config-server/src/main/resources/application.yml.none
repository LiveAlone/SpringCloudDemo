spring:
  application:
    name: config-server

server:
  port: 8887

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LiveAlone/SpringCloudDemo/
          searchPaths: respos
          username: LiveAlone
          password: yao4094230
      label: master
eureka:
  client:
    service-url: http://localhost:8761/eureka/

